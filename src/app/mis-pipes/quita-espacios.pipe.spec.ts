import { QuitaEspaciosPipe } from './quita-espacios.pipe';

describe('QuitaEspaciosPipe', () => {
  it('create an instance', () => {
    const pipe = new QuitaEspaciosPipe();
    expect(pipe).toBeTruthy();
  });
});
